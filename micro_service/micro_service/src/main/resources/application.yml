server:
  port: 0

spring:
  application:
    name: micro-service

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

app:
  id: ${random.uuid}